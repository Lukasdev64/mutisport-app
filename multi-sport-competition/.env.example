# Variables d'environnement pour Better Auth

# URL du serveur d'authentification Better Auth
# En production: https://api.votre-domaine.com
# En développement: http://localhost:3000
VITE_AUTH_URL=http://localhost:3000

# Configuration pour l'authentification sociale (à définir côté serveur)
# Ces variables ne sont PAS exposées côté client pour la sécurité

# Google OAuth (côté serveur uniquement)
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret

# GitHub OAuth (côté serveur uniquement)
# GITHUB_CLIENT_ID=your_github_client_id
# GITHUB_CLIENT_SECRET=your_github_client_secret
