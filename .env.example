# Environment Variables Template

# Supabase Configuration
VITE_SUPABASE_URL=<your_supabase_project_url>
VITE_SUPABASE_ANON_KEY=<your_supabase_anon_key>

# PWA Configuration
VITE_APP_URL=http://localhost:5173
VITE_APP_NAME=Multi-Sport Tournament

# OneSignal Push Notifications
# 1. Create free account at https://onesignal.com
# 2. Create a "Web Push" app
# 3. In Settings > Keys & IDs, copy the App ID
VITE_ONESIGNAL_APP_ID=<your_onesignal_app_id>

# Note: OneSignal SDK automatically allows localhost in development
# Push notifications work on localhost:5173 for desktop browsers

# ===== Supabase Edge Function Secrets =====
# These are set via `supabase secrets set` command, not in .env file
# ONESIGNAL_APP_ID=<same as VITE_ONESIGNAL_APP_ID>
# ONESIGNAL_REST_API_KEY=<from OneSignal Settings > Keys & IDs > REST API Key>
